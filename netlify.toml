# Netlify Configuration File
# This file automatically configures Netlify when the user builds or deploys this repository.

[build]
  command = "npm run build"
  publish = "dist"

# Redirect rules for single page application (SPA) routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
